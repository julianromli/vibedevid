# Plan: Remove AILeaderboardSection from Landing Page

## Objective
Remove `AILeaderboardSection` dari landing page tanpa memecah section lain.

## Changes Required

### File: `app/home-page-client.tsx`

1. **Hapus import statement** (baris 5):
   ```tsx
   import { AILeaderboardSection } from '@/components/sections/ai-leaderboard-section'
   ```

2. **Hapus ErrorBoundary + component** (baris 186-188):
   ```tsx
   <ErrorBoundary sectionName="AI Leaderboard">
     <AILeaderboardSection />
   </ErrorBoundary>
   ```

## Files to Modify
- `app/home-page-client.tsx`

## Verification
After changes:
1. Run `bun tsc --noEmit` - ensure no TypeScript errors
2. Run `bun lint` - ensure no lint errors
3. Run `bun dev` and verify landing page loads without errors

## Impact
- Section "AI Leaderboard" tidak akan tampil di landing page
- Section lain (AIToolsSection, ReviewsSection, dll) tetap berfungsi normal
- Tidak ada dependensi yang rusak karena AILeaderboardSection berdiri sendiri
